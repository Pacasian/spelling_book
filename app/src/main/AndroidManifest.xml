<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.spellingbook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FloatingBar"
            android:label="@string/title_activity_floating_bar"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".numbers">

        </activity>
        <activity android:name=".eachAlphabet" />
        <activity android:name=".alphabets2screen" >

        </activity>

        <activity android:name=".secondscreen"></activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>